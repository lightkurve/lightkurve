steps:
- script: conda env create --quiet --file environment.yml
  displayName: Create Anaconda environment

- script: conda activate lightkurve
  displayName: Activate Anaconda environment

- script: pip install -e .[all,test]
  displayName: 'Install Lightkurve & dependencies'

- script: |
    pytest
  displayName: 'pytest'